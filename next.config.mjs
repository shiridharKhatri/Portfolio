/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        "img.icons8.com",
        "localhost:5000",
        "calm-lime-caiman-hem.cyclic.app",
      ],
      unoptimized: true,
    },
  };
  
  export default nextConfig;
  